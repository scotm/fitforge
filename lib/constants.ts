export const DEFAULT_PAGE_SIZE = 40;

export const defaultWeightUnit = ["kg", "lbs"] as const;
export const defaultDistanceUnit = ["km", "miles"] as const;
export const defaultHeightUnit = ["cm", "inches"] as const;

export const defaultGenderOptions = ["man", "woman", "non-binary"] as const;
